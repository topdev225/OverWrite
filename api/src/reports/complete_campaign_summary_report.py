from src.reports.types import Table


class CompleteCampaignSummaryReport(Table):
    name = "Complete Campaign Summary"

    def generate(self, *args, **kwargs):
        # FIXME: this obviously is an incomplete function that needs to be written
        # report = kwargs.get("report")
        # campaign = report.campaign

        # columns = []
        # rows = []

        # columns += [f"Vendor cost total", "Sell total", "Total sell"]

        pass
